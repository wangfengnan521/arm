# X5A 语音抓取 Android App

按住说话，松手把录音传到主机 `faster-whisper`，再走现有 `/x5a/pick_place`。

默认自动连接：

- `http://192.168.0.50:8000`
- 连不上再试 `https://192.168.0.50:8000`
- 再试 `https://192.168.0.50:8443`

APK 路径：

```text
android/X5AVoice/app/build/outputs/apk/debug/app-debug.apk
```
